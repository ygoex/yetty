{% from "layouts/partials/components/navigation/macro.njk" import navigation %}

<header>
  <a class="home" href="{{ '/' | url }}">{{ metadata.title }}</a>

  {{ navigation({
    name: "Main menu",
    collection: collections.all,
    hasButton: true,
    buttonText: "Contact",
    buttonHref: "mailto:helloygoex@gmail.com"
    })
  }}
</header>
